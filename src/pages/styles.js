import styled from 'styled-components';
import { Row } from 'reactstrap';

export const CoolRow = styled(Row)`
    padding-left: 25px;
`;

export const FilterTagRow = styled(CoolRow)`
    margin-bottom: 1em;
`;

export const JustifiedRow = styled(Row)`
    text-align: justify;
`;